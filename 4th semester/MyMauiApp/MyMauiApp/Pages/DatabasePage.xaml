<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyMauiApp.Pages.DatabasePage"
             Title="Book collection">
    <VerticalStackLayout>
        <CollectionView Margin="5">
            
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Margin="5" VerticalOptions="Center" HorizontalOptions="Start">
                        <Label Text="{Binding Title}" FontSize="20" FontAttributes="Bold"/>
                        <Label Text="{Binding Description}" FontSize="18" FontAttributes="Italic"/>
                        <HorizontalStackLayout Spacing="10" Margin="2">
                            <Label Text="{Binding Year}" FontSize="14"/>
                            <Label Text="{Binding PagesAmount, StringFormat='{PagesAmount} страниц'}" FontSize="14"/>
                            <Label Text="{Binding Author}" FontSize="14"/>
                        </HorizontalStackLayout>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
            
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>